{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "set-domains output",
    "$id": "http://nethserver.org/json-schema/task/output/automx/set-domains",
    "description": "Domains whose routes could not be fully applied (e.g. DNS not resolvable yet -- DESIGN.md 5.6); an empty list means everything applied cleanly. Call get-domains for full per-record DNS status.",
    "type": "object",
    "required": [
        "route_failures"
    ],
    "additionalProperties": false,
    "properties": {
        "route_failures": {
            "type": "array",
            "items": {
                "type": "string"
            }
        }
    }
}
